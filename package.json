{
  "name": "aarogya-grid",
  "version": "1.0.0",
  "description": "Health supply resilience platform for Indian PHCs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write .",
    "seed": "node seed/import.js"
  },
  "devDependencies": {
    "ajv": "^8.12.0",
    "prettier": "^3.1.0"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  }
}
